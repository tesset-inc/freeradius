services:
  freeradius:
    container_name: freeradius
    image: ghcr.io/tesset-inc/freeradius:v1.0.0
    network_mode: host
    restart: unless-stopped
    privileged: true
    env_file:
      - stack.env
    ports:
      - "1812:1812/udp"
      - "1813:1813/udp"
